name: CodeQL

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  schedule:
    - cron: '0 6 * * 1' # 每周一 06:00 UTC

concurrency:
  group: codeql-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    name: Analyze (javascript-typescript)
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write
    strategy:
      fail-fast: false
      matrix:
        language: [javascript-typescript]
    steps:
      # pin: actions/checkout@v4.1.7
      - uses: actions/checkout@v4.1.7
      # pin: github/codeql-action/init@v4.37.8
      - uses: github/codeql-action/init@v4.37.8
        with:
          languages: ${{ matrix.language }}
          #  queries: security-extended,security-and-quality
      # pin: github/codeql-action/autobuild@v4.37.8
      - uses: github/codeql-action/autobuild@v4.37.8
      # pin: github/codeql-action/analyze@v4.37.8
      - uses: github/codeql-action/analyze@v4.37.8
        with:
          category: "/language:${{matrix.language}}"
